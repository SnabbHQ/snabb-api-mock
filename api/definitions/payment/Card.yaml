type: object
properties:
  card_id:
    type: integer
    description: "Unique identifier representing a specific source card."
  fingerprint:
    type: string
    description: "Unique identifier representing a specific source card."
  customer:
    type: string
    description: "Unique identifier representing a specific customer."
  exp_month:
    type: integer
    description: "Expedition month of card."
  last4:
    type: string
    description: "Last four chars of card."
  brand:
    type: string
    description: "Brand of card."
  funding:
    type: string
    description: "Funding of card."
