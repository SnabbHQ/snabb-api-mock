type: object
properties:
  first_name:
    type: string
    description: "First name of the user."
  last_name:
    type: string
    description: "Last name of the user."
  email:
    type: string
    description: "Email address of the user"
  phone:
    type: object
    description: "The phone number of the user."
    allOf:
      - $ref: '#/definitions/Phone'
  thumbnail:
    type: string
    description: "Small profile image URL of the user"
  verified:
    type: boolean
    description: "Flag representing if the user has been verified or not. In case is not verified, the user will have
     restricted access to the API and only certain actions will be allowed."
