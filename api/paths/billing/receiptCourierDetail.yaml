get:
  tags:
  - Billing
  summary: "GET /billing/receiptCourier/{receipt_id}"
  description: "Get Html rendered with receipt of Courier"
  operationId: receiptCourierDetailGET
  security: []
  parameters:
        - name: receipt_id
          in: path
          description: Unique Id for receipt Courier
          required: true
          type: integer
  responses:
    200:
      description: "Receipt Courier HTML rendered"
      schema:
        $ref: '#/definitions/ReceiptCourierDetail'
      examples:
        text/html:
          <html><body>Receipt User HTML </body></html>
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  x-swagger-router-controller: Auth
