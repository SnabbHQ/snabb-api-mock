get:
  tags:
  - Billing
  summary: "GET /billing/receiptUser/{receipt_id}"
  description: "Get Html rendered with receipt of User"
  operationId: receiptUserDetailGET
  security: []
  parameters:
        - name: receipt_id
          in: path
          description: Unique Id for receipt User
          required: true
          type: integer
  responses:
    200:
      description: "Receipt User HTML rendered"
      schema:
        $ref: '#/definitions/ReceiptUserDetail'
      examples:
        text/html:
          <html><body>Receipt User HTML </body></html>
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  x-swagger-router-controller: Auth
