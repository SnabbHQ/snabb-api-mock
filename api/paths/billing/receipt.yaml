get:
  tags:
  - Billing
  summary: "Get user receipts"
  description: "Blah"
  operationId: recetipsGet
  security: []
  responses:
    200:
      description: "Receipt for a given delivery"
      schema:
        $ref: '#/definitions/Token'
      examples:
        application/json:
          {
            "access_token": "J1qK1c18UUGJFAzz9xnH56584l4",
            "token_type": "bearer",
            "expires_in": 2592000,
            "scope": "api"
          }

    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  x-swagger-router-controller: Auth