get:
  tags:
  - Products
  summary: Product Types
  description: "The Products endpoint returns information about the *Uber* products offered at a given location. The
   response includes the display name and other details about each product, and lists the products in the proper
   display order."
  operationId: productsGET
  parameters:
  - name: latitude
    in: query
    description: "Latitude component of location."
    required: true
    type: number
    format: double
    x-example: "12.2330"
  - name: longitude
    in: query
    description: "Longitude component of location."
    required: true
    type: number
    format: double
    x-example: "0.2332"
  responses:
    200:
      description: "An array of products"
      schema:
        type: array
        items:
          $ref: '#/definitions/Product'
    default:
      description: Unexpected error
      schema:
        $ref: '#/definitions/Error'
  x-swagger-router-controller: Products